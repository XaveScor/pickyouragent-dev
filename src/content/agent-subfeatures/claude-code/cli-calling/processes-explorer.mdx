---
agentId: claude-code
featureName: cli-calling
subfeatureName: processes-explorer
---

import ResponsiveImage from "../../../../components/ResponsiveImage.astro";
import processExplorerExample from "./process-explorer.png";
import bashesExample from "./bashes.png";

Claude Code can run tasks in the background and display them. You can view shell details including status, runtime, command, and output. You can also kill processes using the `k` shortcut.

<ResponsiveImage
  src={processExplorerExample}
  alt="Claude Code showing a background task running with 1 bash process indicator"
  caption="Claude Code displays background tasks in the status bar."
/>

If you want to manage already run processes, just type `/bashes` or `/tasks`.

<ResponsiveImage
  src={bashesExample}
  alt="Claude Code shell details showing status, runtime, command and output with option to kill process"
  caption="Claude Code provides detailed shell information and allows killing processes with the k shortcut."
/>

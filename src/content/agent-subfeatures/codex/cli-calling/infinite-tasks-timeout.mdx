---
agentId: codex
featureName: cli-calling
subfeatureName: infinite-tasks-timeout
---

import ResponsiveImage from "../../../../components/ResponsiveImage.astro";
import exampleRunning from "./example-of-running.png";

<ResponsiveImage
  src={exampleRunning}
  alt="Codex stopping a long-running dev server"
  caption="Codex automatically stops long-running processes like dev servers"
/>

Codex cannot run long-running processes such as dev servers indefinitely. When executing commands that keep running (like `pnpm dev`), Codex will automatically stop them after a short timeout.
